--------------------------------------------//Brootforce approch//  space complexity: O(1), time complexity:O(N^2)---------------------------------------------

class Solution {
    public int pivotIndex(int[] nums) {
           int n= nums.length;
       for(int i=0; i<=n-1;i++){

             int leftsum=0;
             int rightsum=0;
        //  for left sum
         for(int j=0;j<i;j++){

            leftsum+=nums[j];
          }  
        //    for rigth sum
          for(int j=n-1;j>i;j--){

            rightsum+=nums[j];
          } 

          if(leftsum==rightsum){
             return i;
          }
       }

       return -1; 
        
    }
        
}


----------------------------------------//Optimal approch// space complexity: O(1), time complexity: O(N)---------------------------------------------------

class Solution{

   public int pivotIndex(int [] nums){

      int leftsum=0;
      int totalsum=0;
      int n=nums.length;


      for(int i=0; i<=n-1 ;i++){
          totalsum+=nums[i];
      }

      for(int i=0; i<n-1 ;i++){

        int rightsum = totalsum-nums[i]-leftsum;

          if(leftsum==rigthsum){
           return i;
          }

         leftsum+=nums[i];
      }

        return -1;
   }
}





























